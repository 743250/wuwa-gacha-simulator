{
  "name": "wuwa-gacha-simulator",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:single": "vite build && node scripts/build-single-html.mjs",
    "smoke": "node scripts/smoke-combat.mjs",
    "check:balance": "node scripts/check-balance.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "push": "node scripts/push.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vitest": "^4.1.9"
  }
}
