# 交接 · 纯抽卡模拟器 APK

临时交接。做完对应条就删。不写进 README。

---

## 1. 用户要什么

用户要一个**单独的抽卡模拟器 APK**，给玩家玩完整的抽卡体验。

具体说：把现有项目里的这些部分抽出来做成 APK——

- 抽卡
- 抽卡分析
- 角色信息
- 商店
- 以及抽卡玩起来必需的东西（卡池时间线、海市、存档）

**不要**战斗，**不要**副本。冒险、编队、日常、深塔、海墟、战斗界面，玩家都不该看到。

玩家打开 APK 应该能：按版本抽、看分析、点开角色看资料、去商店和海市、存档能导出导入。分析页要尽量按用户给的截图做，不是现在侧栏那一小块统计。

工程上已经拍板：**同仓库独立构建**，不要复制成第二个项目。完整版模拟器可以留着开发用；打给玩家的包走纯抽卡入口。

素材：优先找官方 / 灰机 wiki / encore 的图，找不到再生。

---

## 2. 仓库里已经有什么

这不是从零做。本仓库已经是能跑的网页模拟器（`npm run dev`），抽卡闭环在网页里是通的。

**抽卡**

- 卡池时间表 1.0–3.6：`src/data/phases.js`
- 概率、保底、单抽 / 十连：`src/gacha/core.js` `src/gacha/actions.js`
- 翻牌动画：`src/ui/gacha/animation.js`
- 唤取界面：`src/ui/panels/gacha/`（banner、抽卡按钮、角色仓库、海市、记录）
- 时间线 / 选版本：`src/ui/TimelineBar.tsx`

**抽卡分析（有，但不是用户要的那一版）**

- 计算：`src/gacha/analysis.js`（均抽、歪率、生涯称号、分池）
- 界面：唤取页右侧「统计」tab，`src/ui/panels/gacha/StatsTab.tsx`
- 用户给的分析长图在：`docs/plans/architecture/gacha-analysis-reference/screen-1.jpg` … `screen-6.jpg`
- 现有统计页和这张图不是同一套布局

**角色信息**

- 仓库卡片：`RoleGrid`
- 详情弹窗（属性 / 武器 / 声骸 / 共鸣链 / 技能 / 升级 / 故事语音）：`src/ui/panels/roleModal/`
- 90 级面板、元素、武器类型：`src/battle/template.js`（详情页也在用，不是只有战斗在用）
- 共鸣链文案：`src/data/chains/registry.ts`
- 技能介绍：`src/ui/panels/roleModal/skillHints/`
- 立绘：`src/ui/assets/art.ts`
- 故事 / 语音：`src/data/character-lore.json`
- 角色是否做完：`docs/plans/characters/status.md`

**商店 / 海市 / 存档**

- 月相、月卡、礼包：`src/shop/` `ShopPanel`
- 海市珊瑚 / 波纹：`src/exchange/` `ExchangeTab`
- 背包货币材料：`BagPanel`
- 存档 localStorage + JSON 导入导出：`src/save.js`（按钮在商店底部）
- 开局选号（新手 / 自抽 / 买号）：`src/ui/setup/`

**战斗和副本（有，但按需求不进 APK）**

- 顶栏「冒险」、编队、日常、副本、深塔、海墟、战斗全屏都在
- 启动时 `src/init.ts` 会初始化战斗资源和副本
- `src/battle/` 整包都在；打给玩家的包不应加载这些

**角色覆盖（代码盘点）**

卡池已到 3.6。角色条目 57（37 限定 + 5 常驻五星 + 12 四星 + 漂泊者三形态），可进卡池 54。

还缺、玩家会直接看到的：

| 缺口 | 谁 |
|---|---|
| 正式池名 | 秧秧·玄翎、穗穗、清宵、景燃 |
| 立绘 | 清宵、景燃 |
| 卡池横图 | 秧秧·玄翎、穗穗、清宵、景燃 |
| 故事 / 语音 | 秧秧·玄翎、穗穗、清宵、景燃 |
| 技能详情 / 共鸣链文案 | 清宵、景燃（现在是空白或通用 fallback） |
| 3.5 四星陪跑没手写 | 回落成秧秧 / 丹瑾 / 桃祈 |

encore 已有清宵 1413、景燃 1212、秧秧·玄翎 1610、穗穗 1110。漂泊者·导电官方有、模拟器没有，加不加等用户点头。

---

## 3. 已经做了什么（本轮交接之前）

这些是已经落地的，下一轮不用重做：

- 完整网页模拟器能跑：抽卡、养成、战斗、商店都在同一套代码里。
- 用户拍板记下来了：同仓库独立构建；不要战斗 / 副本；分析按截图；素材先找图再生。
- 分析参考图切成 6 张 JPEG 进了仓库，路径见上一节。
- 文档入口收成：`README.md`（按原 AGENTS 正文）、`SPEC.md`、`log.md`、本文件。
- `docs/sources/mechanics` 并进 `mechanisms`；一次性脚本挪到 `scripts/oneoff`。
- **分析页本身没改。** 当时看不清截图，用户说看不清就不要做，所以没有猜着画。
- **纯抽卡入口还没有。** 没有 `dev:gacha`，没有独立 HTML，APK 也没打。
- **3.5 / 3.6 静态缺口还在。** 没有补池名、立绘、lore。
- **没有改抽卡概率，没有改已实装角色战斗数值。**

---

## 4. 还没做完（下一轮按这个做）

顺序不要倒。分析必须先识图。

1. **识图，复刻抽卡分析**  
   对照 6 张参考图逐块做。扩展 `analysis.js`（总消耗、生涯评分、四池、时间线），分析做成独立页。不要改抽卡概率。不要在旧 StatsTab 上凑合。看不清就停。

2. **纯抽卡入口能在浏览器跑**  
   `index.gacha.html` + 独立启动；`npm run dev:gacha` / `build:gacha`。  
   `initGachaApp()` 不调战斗 / 副本初始化。顶栏没有「冒险」。能抽、能看角色、能进商店、能看分析。

3. **角色信息给玩家能看**  
   补上一节缺口表。清宵 / 景燃技能和链可以写「已收录，战斗数据待补」，禁止编造倍率或 chain effect。不要改已实装角色数值。

4. **商店 / 背包像给玩家用的**  
   去掉「去副本打声骸」死链。存档导出放在找得到的地方。

5. **打 APK**  
   只打包 `build:gacha`。竖屏。能装、能抽、能备份存档。

不要做：猜分析像素；改已实装角色战斗数值；为清宵 / 景燃伪造 effect；把仓库拆成两份项目；先把战斗做完再交 APK。

交差：玩家装上 APK 能抽、能看分析（对得上截图结构）、能看角色、能用商店和海市、能存档；看不到战斗和副本；清宵 / 景燃能抽到且有立绘。
